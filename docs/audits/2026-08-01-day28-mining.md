# 2026-07-28 V-day mining — new-scenario hunt + v3 engine audit

**Date:** 2026-08-01 · **Data:** 15 wall-to-wall NQ recordings 09:30–12:01 + the v3 strategy's zero-trade session log + `discriminator_log.jsonl`
**Method:** 21-agent workflow (3 miners → 2 adversarial verifiers per candidate → cross-scan). 9 candidates proposed; the verifiers reproduced every headline number and then cut hard. Nothing passed 2-0 as a tradeable rule — correct on 2 days of data — but four survive as log-only candidates with corrected specs, and the engine audit produced structural findings.

## The day, verified

Two clean legs: −388.5 pt (09:30 → 27603.50 @ 10:16:52) then +459 pt in 92 min. The "everything reverses" look is a resolution artifact: at a 30 pt ZigZag the day has 95 pivots, path efficiency 0.019, ~78% of legs retraced ≥70% — that is what floods the outcome stream with REVERSAL labels. The V-bottom itself was NOT an event: the quietest 15-min block of the down move, no big cluster at the low.

## Engine audit: zero trades was CORRECT — and the funnel's real bottleneck is B2

0/141 clusters passed the frozen funnel (independently verified). Marginal pass rates on 07-28: bars 90.8%, **K1 12.8%** (median 15-min range 167.8 pt vs the 120 ceiling), K2 29.8%, K3 24.1% (median |d15| 489), B1 89.4%, **B2 2.8%** (median break_pts −71; only 4/141 at ≥6 pt). The proposed session-relative K1'/K3' fix was **refuted as scoped**: replayed causally it changes nothing on 07-28 (its K-only candidates still die at B2, 2/3 are stop-losers) and it *collapses* 07-27's K-only set. Redirect: any future re-registration should re-diagnose **B2**, not relativize K1/K3. Also confirmed: the corpus as gated only accrues rows on low-vol sessions — a selection bias to keep in mind when reading the ≥22/30 gate.

**"Stall ⇒ fade" took a direct hit.** On both days, a post-sweep stall (no progress ≥1 pt by t_end+5s; also t2.levels_traded==0, 43/141 triggers) favored CONTINUATION — fading stalls won 2/25 combined. This is evidence AGAINST the intuition inside the VirginExtension+T2 fade path; T2's reversal vote stands only on the original R evidence. Watch it in the corpus; the first live VirginExtension (BUY 146 @ 11:18, brk 21, T2 zero levels) resolved UNRESOLVED.

## Surviving candidates (all LOG-ONLY; none tradeable yet)

1. **OSB-Fade (narrowed)** — fade a genuine **single** Last print ≥50 lots that does NOT set a new 300s price extreme (brk300≤0); 5s lag, 20/40 bracket. The only candidate with a non-refuted verdict; the second verifier's narrowing: DROP the 20ms-aggregation clause (it admits sweep fragments — aggregated-only leg has no edge). ~10 independent episodes / 2 days, ≈+0.5R episode-mean, leave-one-out stable. Genuinely novel (single-print absorption; neither cluster- nor book-based). The brk300≤0 gate was chosen post-hoc → pre-register before the next session.
2. **An flow-agreement veto** — An = (trailing-60s aggressor delta × cluster side) / trailing-60s volume, dimensionless (transfers NQ↔MNQ). The **veto half survives every adversarial check: never fade a qualifying cluster when An ≥ 0.10** (fade at An≥0.10: 11-17% win both days). The follow-entry half is positive both days (raw-verified 07-28: n=45, 55.6%, +0.67R avg) but UNVALIDATED after independence collapsing. Adjacent to killed D1 — differs by normalization + relative signing + veto-use; keep that distinction explicit.
3. **EXC — extension continuation** (anti-K1 branch): cluster printing AT/through the trailing 15-min extreme in a non-balance window → follow the aggressor. Numbers reproduce exactly (07-28 n=29 raw +18.1 pt; single-position sim +137 pt on the zero-trade day), causal — but n collapses under independence, both days are trend days, sell side only provisional. This is the trend-day complement the engine lacks.
4. **Failed no-touch retest of the leg low → LONG (the "caso 4" candidate)** — the V-bottom anatomy, n=1, geometry verified to the tick: L0 27603.50, L1 +10 ticks 267 s later never touching, **zero buy volume in the base's last 5 ticks** (40 contracts, all sell-aggressed), −186 band delta, trigger = +110-delta/188-contract burst 15 s after L1; stop under L0 −2. Explicitly NOT case 1 (which needs the low TAKEN OUT; this needs the retest to FAIL SHORT of it). Both verifiers: core causal claim survives; corrected spec recorded (live running-minimum L1, fixed L0+12pt band). The catalog's first long-side entry candidate.
5. **S(t) outcome day-state** — rolling mean of the last K=10 resolved cluster labels. Keep ONLY as a free logged covariate; all "validated" orderings retracted (MNQ contamination + declustering needed).

## Killed

- **OSB-Level** (block price as later S/R): properly counted it fires 213×, +0.04R — flat; the reported 26/+0.38R was a detector artifact.
- **OSB-Repeat** (same-side block repeated): 7/7 wins collapse to n_eff≈2 episodes.
- **Stalled-extension as a standalone entry** (nested in EXC; the stall filter adds nothing to follow-side).
- **Relative K1'/K3' re-parameterisation as scoped** (see engine audit).

## Data hygiene (done)

The 3 MNQ manual recordings (07-30 session) lived in the 2026-07-27 folder and contaminated one analysis → moved to `recordings/2026-07-27/mnq_manual/`. Rule going forward: analysis scripts must filter `meta.instrument`.

## Recommended next steps

1. Add the free covariates to the v3 trigger records: `an60`, `S(t)`, `exc_candidate` (break_pts≥0 flag already derivable), `osb` events in a parallel log — all log-only, no frozen rule touched. The corpus then scores them for free.
2. Implement candidate 4 (failed no-touch retest → long) as the library's first LogOnly long setup, per the corrected spec.
3. Keep recording. Still zero genuine RANGE days in the corpus — both sessions are trend days; K1's band has never actually been exercised beyond the one 09:56 event.
4. Recorder still needs the StopRunTicks retune (wall-to-wall metronome again on 07-28).
